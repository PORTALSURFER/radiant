# Central declarative animation (OPT-1413)

Final source: `6f7c6d64171c5749790e039981d5ed329827a066`. Local date: 7 September 2026 (Europe/Amsterdam); tool logs ran on 6 September UTC. The full library suite ran at `0f59bd203bba70a4d973d985971e1a10641ae118` after integrating ResourceView main. Later changes moved the public animation exports onto the existing application facade and made behavior-preserving strict-lint changes. Integration, examples and doctests ran after the facade correction; strict Clippy, no-default, formatting, diff and final fixture ran on the final source tree.

The central runtime owns bounded interpolation and shared feedback clocks, accepted owner identity, retargeting, completion, removal, hidden/recovery pause and reduced-motion static state. Immutable capabilities receive sampled values during actual paint and custom layout. A virtual clock is installed before constructor admission. Changed geometry reuses accepted application projection; paint samples invalidate base paint without requiring layout. No retained paint-segment speedup or foreground native performance result is claimed.

Validation: 4313 library tests passed (8 existing ignored), plus all integration, example and doctests; see logs for exact counts. Strict Clippy, docs, no-default, formatting and diff checks passed. The public headless example has five tests for visible paint, geometry, retargeting, removal, no frame messages/projection, reduced motion and hidden shared phase. Additional tests cover 1024-property indexed sample application with reused container storage, nested scenes, duplicate/capacity rejection, native visibility/occlusion/recovery composition, and engine edge cases.

Independent review identified hidden auxiliary-window advancement and quadratic sample scans. Both were fixed and re-reviewed with no remaining meaningful finding. Sample application now indexes owner-contiguous samples and reuses values storage. Native hidden and recovery behavior is verified through headless/controller/adapter tests; no foreground window was launched for this ticket.
