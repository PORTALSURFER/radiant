# Typed drop-target feedback — OPT-1363 partial delivery

Source: `3add78494353e198637e3da4ffba9257df6d435e`. C/PASS and A/PASS are scoped to local compilation and deterministic core/paint assertions. H/NOT_RUN and M/NOT_RUN: no native visual or manual acceptance run was attempted.

Opt-in accepted/pending/rejected outlines use the active theme, existing runtime overlay phase, exact layout/source evidence, ancestor/viewport clipping, and shared drag teardown. Paint does not negotiate or emit messages. Compatible target-event projection is requalified during input.

Validation: 5524 library/integration tests (8 existing ignored), 295 example tests, five focused public feedback regressions, 20 doctests (1 ignored), strict all-target/all-feature Clippy, warning-denied docs, host no-default-features, formatting, and diff checks. The tests exercise negotiation colors, compatible target-event updates, opt-in default, no-projection pointer motion, clipping, resize, focus loss, and removal during an event.

Insertion-position feedback, autoscroll, cross-window payloads, and external offers remain open. Candidate evidence does not claim canonical shipment or three-platform acceptance. The existing compact-unwind linker warning remains non-fatal.
