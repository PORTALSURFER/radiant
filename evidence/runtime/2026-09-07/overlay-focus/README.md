# Overlay focus, dismissal, and current-layout anchor evidence

Source: `b6c65148`, integrated with the cleanup-only main change at `6f61fb6d07db2e3031299f49ce799a2036c7a0a8`. The integration changes only `.gitignore`; validated source, tests, examples, and documentation are identical.

The final locked-dependency run passes 5,554 library/integration tests (8 existing ignored), 294 example tests across 82 binaries, and 20 doctests (1 ignored). The focused overlay run passes 218 tests. All-target/all-feature Clippy with warnings denied, documentation with rustdoc warnings denied, the no-default-features library build, formatting, and diff whitespace checks pass. Logs and command outcomes are included here.

The implementation composes existing focus authority with bounded overlay source evidence. It covers modal sequential/spatial/explicit/semantic focus trapping, exact-incarnation prior focus and fallback, nested restoration and dismissal ordering, focused-editor Escape precedence, capture/composition retirement, and reentrant publication fences. Current-pass anchor placement covers below/above, flip/clamp, resize, logical scale, unique target identity, partial versus complete clipping, missing/forward/self targets, nested dependencies, bounded source/clip evidence, and complete foreground/input/semantic omission. The floating-overlay example uses the public anchored-layer API.

Anchored and newly materialized modal admission uses the final accepted layout. Invisible groups never probe the incumbent editor. A veto omits the new modal and descendants while preserving the accepted base and compatible focus; ordinary unanchored modal veto remains before runtime mutation. One final membership publication prevents surviving anchored scopes from losing and regaining focus during refresh. Independent review found and corrected false pre-layout admission, a surviving-scope double-publication bug, and a preflight-ordering regression. The final focused and full suites pass after those corrections. Deep-view 256/257-level regressions pass in the complete run.

Native keyboard adapter regressions use controlled/headless inputs. No foreground native window, DAW session, or OS clipboard was accessed. This bundle does not claim manual native-host or audible acceptance. Existing ignored tests and the toolchain compact-unwind linker warning remain explicit rather than counted as passing acceptance.
